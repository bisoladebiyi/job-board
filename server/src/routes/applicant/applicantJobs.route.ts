import express from "express"

export const router = express.Router();

router.get("/apply", () => { })
router.get("/applied", () => { })
router.get("/save", () => { })
router.get("/saved", () => { })